#!/usr/bin/env bash
set -euo pipefail

readonly repo_root="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd -P)"

export PYTHONDONTWRITEBYTECODE=1
exec sudo env PYTHONDONTWRITEBYTECODE=1 \
  python3 "$repo_root/verify/public.py"
